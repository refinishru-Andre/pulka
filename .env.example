# ШАБЛОН. Скопируйте этот файл рядом под именем .env и впишите ключ.
#
#   copy .env.example .env      (в проводнике: скопировать и переименовать)
#
# .env закрыт .gitignore — в репозиторий он не попадёт.
# НИКОГДА не вставляйте содержимое .env в чат.
#
# SERVICE_KEY — ключ service_role вашего Supabase на 178.154.204.13.
# Нужен ТОЛЬКО скриптам из папки scripts/ (заморозка и пересчёт партий).
# Само приложение им не пользуется: там ключ anon, он публичный и лежит в коде.
#
# Вписывать сразу после знака = , без кавычек и без пробелов.

SERVICE_KEY=
